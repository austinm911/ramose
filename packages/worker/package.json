{
  "name": "@ripple/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ripple peer Worker: HTTP API (/transact /query /pull /db-info), datalog at the edge over cached segments + replica novelty; exports the DO classes.",
  "main": "src/index.ts",
  "dependencies": {
    "@ripple/core": "workspace:*",
    "@ripple/storage": "workspace:*",
    "@ripple/transactor": "workspace:*",
    "@ripple/replica": "workspace:*",
    "effect": ">=4.0.0-beta.105",
    "jose": "^6.0.0"
  }
}
