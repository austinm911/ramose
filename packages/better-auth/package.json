{
  "name": "@ripple/better-auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Better Auth plugins for Ripple: `rippleToken` adds the mint route that signs the workspace-scoped JWT a Ripple peer verifies (same JWKS as the jwt plugin), and `rippleTokenClient` pairs it with `Ripple.token.jwt` in the browser.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts"
  },
  "dependencies": {
    "@ripple/alchemy": "workspace:*",
    "@ripple/core": "workspace:*",
    "zod": "^4.3.6"
  },
  "peerDependencies": {
    "better-auth": ">=1.6.0"
  },
  "devDependencies": {
    "better-auth": "^1.6.30",
    "effect": ">=4.0.0-beta.105",
    "jose": "^6.1.3"
  },
  "sideEffects": false
}
