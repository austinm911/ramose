# Todos

React + `Session.connect` + `db.live`. From the repo root:

```sh
bun alchemy dev examples/todos/alchemy.run.ts
VITE_RIPPLE_URL=http://localhost:8787 bunx vite examples/todos
```

The first command is the peer (miniflare). The second is the UI.
